/**
 * Entry point for `npm run fetch:yahoo`.
 *
 * Not implemented yet — this is where the Yahoo Fantasy Sports OAuth client,
 * league/team/matchup/player fetchers, and normalization pipeline will live
 * (see scripts/yahoo/ and scripts/transform/ in the project plan).
 *
 * Until then, the frontend runs entirely against the bundled mock data in
 * public/data/mock/ — no Yahoo credentials required. See README.md.
 */
console.log("[fetch:yahoo] Yahoo ingestion is not implemented yet in this phase of the build.");
console.log("The frontend currently runs against mock data — try `npm run dev`.");
process.exit(0);
