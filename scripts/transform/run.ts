/**
 * Entry point for `npm run generate`.
 *
 * Not implemented yet — this is where normalized Yahoo data gets turned into
 * derived statistics (superlatives, records, power rankings, luck, strength
 * of schedule) and written to data/current/ and data/historical/.
 */
console.log("[generate] Derived-statistics generation is not implemented yet in this phase of the build.");
process.exit(0);
